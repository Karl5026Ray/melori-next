# Capacitor native projects are generated locally via `npx cap add ios`.
# They contain machine-specific paths and signing config; regenerate rather than commit.
/ios/
/android/
/node_modules/
/www/assets/generated/

# Byte-cache from scripts/mark_utils.py, imported by the icon pipeline.
__pycache__/
